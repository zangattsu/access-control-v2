# Access Control

Projeto inicializado como repositório Git próprio na pasta `access-control`.

## Como começar
- Instale dependências quando houver um `package.json`.
- Use `git add .` e `git commit` para versionar mudanças.
- A branch padrão é `main`.

## Estrutura
- `.gitignore` configurado para Node/Next.